# Đặt file này ở thư mục GỐC
[phases.setup]
pkgs = ["nodejs-20_x"] # Ép buộc dùng Node.js 20

[start]
run = "npm start"

[build]
base_directory = "backend" 
# DÒNG NÀY ĐỂ BÁO CHO RAILWAY BIẾT CODE CHÍNH NẰM TRONG THƯ MỤC 'backend'
